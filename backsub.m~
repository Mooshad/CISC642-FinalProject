one = rgb2gray(imread('1.JPG'));
two = rgb2gray(imread('2.JPG'));
three = rgb2gray(imread('3.JPG'));
four = rgb2gray(imread('4.JPG'));

[r,c] = size(one)
result = zeros(r,c);

imadd(one,two,'uint16')

imshow(result);
figure
imshow(imsubtract(four,result));
