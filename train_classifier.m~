function [ data_out ] = train_classifier( tdata,classifier )
%trains a classifier
[r,c] = size(tdata);
for col=1:r
    dat = 
    tdata{col,end} = classsify(tdata{col,:},classifier);
end
data_out=tdata;
end

